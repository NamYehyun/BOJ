#include <iostream>

using namespace std;


int main(void) {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);

    int n;
    cin >> n;

    cout << (n == 0 ? "YONSEI" : "Leading the Way to the Future") << endl;

    return EXIT_SUCCESS;
}

